pkgname=repetier-host
pkgver=0.85c
pkgrel=1
pkgdesc="The software driving your 3D printer"
arch=('any')
license=('APACHE')
depends=('mono' 'slic3r')
source=("repetier.${pkgver}.tgz::http://www.repetier.com/?wpdmact=process&did=MzUuaG90bGluaw==")
md5sums=('07a74f9c65eed5b6bd77287ce180b199')

prepare() {
	 cd ${srcdir}/RepetierHost
	 sh configureFirst.sh
}

build() {
	 :
}

package() {
	 :
}
